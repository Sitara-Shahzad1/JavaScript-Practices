let arr = [22,1, 44, 3,4,6,7]
let reversed = arr.reverse(" ");
console.log(reversed);

// ===========


let decsorted = arr.sort((a,b) => b-a); // descending
console.log(decsorted);

let asssort = arr.sort( (a,b) => a-b);
console.log(asssort);
