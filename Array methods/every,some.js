let arr = [1,2,3,4,5];
let every = arr.every( num => num %2);
console.log(every);
// it checks if every element of the array passes a test or not 

let some = arr.some( num => num %2) ;
console.log(some);