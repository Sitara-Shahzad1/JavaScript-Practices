let arr = [1,2,3,4,5,66,5,7];

let mid = arr.indexOf(5);
console.log(mid);

let last = arr.lastIndexOf(); // it will return -1 bcz no index is given so it will be    undefined = -1
console.log(last);

let last1 = arr.lastIndexOf(5); // same word from last indexes 
console.log(last1);